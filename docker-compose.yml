services:
  backend:
    build: .
    container_name: annotation-backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend/app:/app/app:ro
      - ./data:/app/data
    environment:
      - APP_ENV=dev
